export const GATEWAY_API_BASE_URL = 'http://localhost:8085';

// Legacy URLs for backward compatibility (redirected through gateway)
export const ORDER_API_BASE_URL = `${GATEWAY_API_BASE_URL}/orders`;
