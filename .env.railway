# ========================================
# VARIABLES PARA RAILWAY - NO SUBIR A GITHUB
# ========================================
# Este archivo es solo de referencia.
# Las variables reales se configuran en Railway Dashboard.

# PostgreSQL (Railway genera esto automáticamente)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Redis (Railway genera esto automáticamente)
REDIS_URL=${{Redis.REDIS_URL}}

# JWT Secret (CAMBIAR en producción)
JWT_SECRET=logitrack_jwt_secret_super_seguro_2024_cambiar_en_produccion

# URLs de servicios internos (Railway networking)
USER_SERVICE_URL=http://user-service.railway.internal:8080
ORDER_SERVICE_URL=http://order-service.railway.internal:8080
GEO_SERVICE_URL=http://geolocation-service.railway.internal:8080
AI_SERVICE_URL=http://ai-service.railway.internal:8080

# URL del API Gateway (se genera después del deploy)
# Reemplazar XXXX con tu dominio real de Railway
REACT_APP_API_URL=https://api-gateway-production-XXXX.up.railway.app

# Puerto (Railway usa 8080 internamente)
PORT=8080
